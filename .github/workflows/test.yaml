on:
  - workflow_dispatch
  - pull_request

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: pnpm/action-setup@v3
      - name: Install dependencies
        run: pnpm
      - name: Run tests
        run: pnpm test
