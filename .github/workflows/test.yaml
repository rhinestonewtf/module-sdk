on:
  - workflow_dispatch
  - pull_request

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: pnpm/action-setup@v3
        with:
          version: 8
          run_install: true
      - name: Run tests
        run: pnpm test
